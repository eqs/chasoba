{
  "name": "chasoba",
  "version": "1.0.0",
  "description": "chasoba lang",
  "main": "index.js",
  "scripts": {
    "build": "tsc --project .",
    "dev": "tsc --project . && node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "eqs",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
